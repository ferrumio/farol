---
title: Getting started
weight: 10
---

# Getting started

1. Add markdown files under `docs/`.
2. Configure your site in `farol.toml`.
3. Run `farol serve` to preview locally.
4. Run `farol build` to produce the static site in `site/`.
